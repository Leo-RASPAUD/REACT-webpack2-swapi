import React from 'react';
import { render } from 'react-dom';
import '../assets/swapi-react.css';

const initRepo = 'Init repo';

render(
    <div>
        {initRepo}
    </div>,
    document.getElementById('root'),
);
