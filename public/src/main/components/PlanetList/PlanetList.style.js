import blue from 'material-ui/colors/blue';

const planetListStyle = {
    width: '200px',
    height: '550px',
};

const listStyle = {
    width: '200px',
};

const iconStyle = {
    color: blue,
};

const planetList = {
    width: '250px',
};

const circularProgress = {
};

export default {
    planetListStyle,
    listStyle,
    iconStyle,
    planetList,
    circularProgress,
};
