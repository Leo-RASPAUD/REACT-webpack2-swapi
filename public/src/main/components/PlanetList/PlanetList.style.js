import { blue500 } from 'material-ui/styles/colors';

const planetListStyle = {
    width: '200px',
};

const listStyle = {
    width: '200px',
};

const iconStyle = {
    color: blue500,
};

const planetList = {
    width: '250px',
};

const circularProgress = {
    marginTop: '8px',
};

export default {
    planetListStyle,
    listStyle,
    iconStyle,
    planetList,
    circularProgress,
};
